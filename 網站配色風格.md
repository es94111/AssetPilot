# AssetPilot 網站配色風格

本文件定義 AssetPilot 網站的主要視覺配色，供首頁、公開資訊頁、登入頁與 App 內頁維持一致風格。現有實作主要位於 `app/globals.css`，Tailwind token 位於 `@theme` 區塊。

## 設計方向

AssetPilot 的視覺語言是「深色金融控制台 + 玻璃質感卡片」。公開首頁採深色科技感，App 內頁保留清楚可讀的淺色工作區，並支援黑色模式。

核心感受：

- 專業：財務資料需要穩定、可信任。
- 清楚：金額、表格、報表必須優先可讀。
- 現代：使用柔和漸層、玻璃霧面、低飽和背景。
- 安全：深色公開頁面搭配藍、綠光暈，呼應自架與加密。

## 品牌主色

| 用途 | 色碼 | 說明 |
|------|------|------|
| Primary | `#4f6ef7` | 主要按鈕、焦點狀態、重要互動 |
| Primary Light | `#7b93fa` | hover、柔和背景、輔助高亮 |
| Primary Dark | `#3b55d9` | primary hover / active |
| Sky Accent | `#38bdf8` / `#bae6fd` | 首頁與公開頁連結、科技感高亮 |
| Emerald Accent | `#10b981` / `#a7f3d0` | 成功、自架、安全、正向提示 |
| Amber Accent | `#f59e0b` / `#fde68a` | 警告、注意事項 |
| Danger | `#f43f5e` | 刪除、錯誤、危險操作 |

## 語意色

| 語意 | 色碼 | 使用位置 |
|------|------|----------|
| 收入 Income | `#10b981` | 收入數字、收入圖表、正向金額 |
| 支出 Expense | `#f43f5e` | 支出數字、支出圖表、負向金額 |
| 淨值 Net | `#3b82f6` | 淨額、總覽、統計摘要 |
| 今日 Today | `#f59e0b` | 今日標記、待處理提醒 |
| 成功 Success | `#10b981` | 成功 toast、完成狀態 |
| 危險 Danger | `#f43f5e` | 刪除、錯誤訊息、破壞性動作 |

## 白色模式

白色模式主要用於 App 內頁、表格、表單與長時間操作場景。

### 背景

| Token | 色碼 | 說明 |
|-------|------|------|
| App Background | `linear-gradient(135deg, #dde5ff 0%, #eef2ff 30%, #e0f2fe 70%, #dcfce7 100%)` | App 外層背景，柔和藍綠漸層 |
| Page Background | `#f4f6fa` | 一般頁面底色 |
| Surface | `#ffffff` | 卡片、表格、表單主要底色 |
| Surface Glass | `rgba(255, 255, 255, 0.72)` | 玻璃質感卡片 |
| Glass Border | `rgba(255, 255, 255, 0.85)` | 玻璃卡片邊框 |

### 文字

| Token | 色碼 | 說明 |
|-------|------|------|
| Text | `#1a1d26` | 主要文字、標題 |
| Text Secondary | `#5c6370` | 次要文字、描述 |
| Text Muted | `#94a3b8` | 補充資訊、placeholder、弱化標籤 |

### 邊框與陰影

| Token | 值 | 說明 |
|-------|----|------|
| Border | `#e4e7ec` | 表格分隔、input 邊框 |
| Shadow Glass | `0 4px 16px rgba(79, 110, 247, 0.10), 0 1px 4px rgba(0, 0, 0, 0.06)` | 一般卡片 |
| Shadow Glass MD | `0 8px 32px rgba(79, 110, 247, 0.12), 0 2px 8px rgba(0, 0, 0, 0.08)` | 浮層與重要卡片 |
| Shadow Glass LG | `0 12px 48px rgba(79, 110, 247, 0.15), 0 4px 16px rgba(0, 0, 0, 0.10)` | Modal、登入卡片 |

### 白色模式使用規則

- 主要操作使用 `#4f6ef7`，hover 使用 `#3b55d9`。
- 卡片使用 `Surface Glass`，不要使用純灰大面積背景。
- 長表格與表單使用白底，確保可讀性高於裝飾性。
- 收入、支出、淨值一定使用語意色，不要任意替換。
- 警告區塊使用 amber，錯誤與刪除使用 danger。

## 黑色模式

黑色模式用於使用者偏好深色介面，也用於首頁與公開資訊頁的主要品牌印象。

### 背景

| Token | 色碼 | 說明 |
|-------|------|------|
| Dark App Background | `linear-gradient(135deg, #0c0f16 0%, #111827 50%, #0c1a2e 100%)` | App 深色背景 |
| Public Background | `#020617` | 首頁與公開資訊頁背景 |
| Dark Surface | `#151922` | 深色卡片、表單底色 |
| Dark Surface Glass | `rgba(21, 25, 34, 0.78)` | 深色玻璃卡片 |
| Dark Glass Border | `rgba(255, 255, 255, 0.10)` | 深色玻璃邊框 |

### 公開頁背景光暈

首頁與公開資訊頁使用相同背景光暈：

```css
radial-gradient(circle at top, rgba(59, 130, 246, 0.18), transparent 32%),
radial-gradient(circle at 80% 20%, rgba(16, 185, 129, 0.14), transparent 24%)
```

### 文字

| Token | 色碼 | 說明 |
|-------|------|------|
| Dark Text | `#e8eaef` / `#ffffff` | App 主要文字 / 公開頁標題 |
| Dark Text Secondary | `#9ca3b4` / `#cbd5e1` | App 次要文字 / 公開頁內文 |
| Dark Text Muted | `#8b94a3` / `#94a3b8` | 補充資訊、標籤 |

### 深色卡片

| 用途 | 建議值 |
|------|--------|
| 公開頁卡片背景 | `rgba(255, 255, 255, 0.05)` |
| 公開頁深色 chip | `rgba(15, 23, 42, 0.70)` |
| 公開頁邊框 | `rgba(255, 255, 255, 0.10)` |
| 公開頁陰影 | `0 24px 80px rgba(0, 0, 0, 0.22)` |
| 公開頁 blur | `blur(18px)` |

### 黑色模式使用規則

- 大面積背景使用接近黑的 slate，不要使用純黑 `#000000`。
- 標題用白色，內文用 `#cbd5e1`，避免全部純白造成刺眼。
- 互動連結使用 sky 色系，hover 可轉白。
- 成功、安全、自架相關提示使用 emerald。
- 警告提示使用 amber，但背景要透明化，避免過亮。
- 卡片邊框使用 `white/10`，不要使用高對比白線。

## 元件配色規則

### 按鈕

| 類型 | 白色模式 | 黑色模式 |
|------|----------|----------|
| Primary | `#4f6ef7` 背景、白字 | `#4f6ef7` 背景、白字 |
| Primary Hover | `#3b55d9` | `#7b93fa` 或 `#3b55d9`，依背景亮度 |
| Secondary | 白底、灰邊框、深色文字 | 透明底、`white/15` 邊框、淺色文字 |
| Danger | `#f43f5e` 或 danger tint | `#f43f5e` tint，避免大面積實心紅 |

### 卡片

- App 卡片：使用 `var(--surface-glass)`、`var(--glass-border)`、`var(--shadow-glass)`。
- 公開頁卡片：使用 `rgba(255,255,255,0.05)`、`white/10`、深色玻璃陰影。
- 重要卡片可使用更大的圓角，例如 `24px` 到 `28px`。

### 表單

- input 背景使用 `Surface` 或 `Dark Surface`。
- focus ring 使用 `rgba(79, 110, 247, 0.10~0.12)`。
- 錯誤訊息使用 danger 色，不用 amber。

### 表格

- 表頭使用 muted 文字。
- 表格線使用 `Border` 或 `white/10`。
- 金額欄位必須套用 income / expense / net 語意色。

### 提示框

| 類型 | 背景 | 邊框 | 文字 |
|------|------|------|------|
| Info | `rgba(14, 165, 233, 0.12)` | `rgba(255,255,255,0.10)` 或 sky tint | `#bae6fd` |
| Success | `rgba(16, 185, 129, 0.12)` | emerald tint | `#a7f3d0` |
| Warning | `rgba(245, 158, 11, 0.14)` | `rgba(251, 191, 36, 0.25)` | `#fde68a` |
| Error | danger tint | danger border | danger text |

## Tailwind 對應

常用 Tailwind class：

| 用途 | Class |
|------|-------|
| 公開頁背景 | `bg-slate-950 text-white` |
| 公開頁弱文字 | `text-slate-300` |
| 公開頁 muted | `text-slate-400` |
| 玻璃卡片 | `border border-white/10 bg-white/5 backdrop-blur` |
| 深色 chip | `border border-white/10 bg-black/10 text-slate-200` |
| 主 CTA 白底 | `bg-white text-slate-950 hover:bg-slate-200` |
| 次 CTA | `border border-white/15 text-slate-100 hover:bg-white/5` |
| Sky accent | `text-sky-300`, `bg-sky-400/10`, `border-sky-400/20` |
| Emerald accent | `text-emerald-200`, `bg-emerald-400/10`, `border-emerald-400/25` |

## 不建議使用

- 不要在公開頁使用白底大面積卡片，會和首頁割裂。
- 不要在黑色模式使用純黑背景加純白邊框，對比太硬。
- 不要用隨機紫色或粉色作為主要 CTA，品牌主色是藍色。
- 不要把收入、支出語意色改成其他顏色。
- 不要在同一張卡片內混用過多 accent，最多一個主 accent。

## 實作來源

目前主要 token 來源：

- `app/globals.css` 的 `@theme` 區塊。
- `app/globals.css` 的 `:root` 與 `.dark-mode` CSS variables。
- `app/page.tsx` 的首頁深色公開頁風格。
- `app/globals.css` 的 `.public-info-page` 公開資訊頁共用風格。
