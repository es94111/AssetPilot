services:
  asset-manager:
    build: .
    container_name: asset-manager
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
      - DB_PATH=/app/data/database.db
      - ENV_PATH=/app/data/.env
      - JWT_EXPIRES=7d
      - GOOGLE_CLIENT_ID=
