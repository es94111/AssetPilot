'use client';

export default function ApiCreditsClient({ user }: { user: any }) {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold text-slate-900 mb-2">API 使用與授權</h1>
      <p className="text-slate-600">此頁面正在遷移至新的前端架構，敬請期待。</p>
    </div>
  );
}
