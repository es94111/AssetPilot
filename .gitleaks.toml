title = "AssetPilot Gitleaks config"

[extend]
useDefault = true

[[allowlists]]
description = "Generated Next.js build artifacts can contain deterministic hashes that trigger generic-api-key false positives."
targetRules = ["generic-api-key"]
paths = [
  '''^\.next/''',
  '''^build/''',
]
