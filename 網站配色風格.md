# AssetPilot 網站配色風格

本文件定義 AssetPilot 網站的主要視覺配色。設計方向為「Warm Console」——溫暖柔和的財務工作台。現有實作位於 `app/globals.css`（`@theme`、`:root` 與 `.dark-mode` 區塊），token 值以該檔為單一來源。

## 設計方向

AssetPilot 的視覺語言是「暖棉紙表面 + 暖赭品牌色」。App 內頁像攤在棉紙上的帳本：清楚、柔軟、長時間使用不疲勞；暗色模式則是「暖夜」——暖黑而非藍黑。

核心感受：

- 溫暖：暖中性的表面色，告別冷藍科技感。
- 專業：金額、表格、報表優先可讀，對比全數 ≥ 4.5:1。
- 親和：柔和圓角階梯（10 → 16 → 20px）、低調微互動、無彈跳動效。
- 秩序：一個主 accent、語意色固定、字體層級明確。

## 品牌主色（Warm Console）

| 用途 | 色碼 | 說明 |
|------|------|------|
| Primary | `#b0521c` | 主要按鈕 hover 深化、焦點、連結、重要互動 |
| Primary Solid | `#a8481a` | 實心按鈕底色、強調塊 |
| Primary Light | `#d98a4a` | 暗色 hero 漸層端點、柔和高亮 |
| Primary Dark | `#994215` | primary hover / active |

漸層 hero：`linear-gradient(135deg, #994215 0%, #b0521c 52%, #d98a4a 100%)`。

## 語意色

| 語意 | 亮色模式 | 暗色模式（暖夜） | 使用位置 |
|------|----------|------------------|----------|
| 收入 Income | `#1e6b52`（松綠） | `#6cc29b` | 收入數字、收入圖表、正向金額 |
| 支出 Expense | `#b3372f`（磚紅） | `#e08279` | 支出數字、支出圖表、負向金額 |
| 淨值 Net | `#8a5a1f`（赭金） | `#d3a35c` | 淨額、總覽、統計摘要 |
| 今日 Today | `#b45309` / 文字 `#8a4a0f` | 文字 `#e5b567` | 今日標記、待處理提醒 |
| 成功 Success | `#1e6b52` | `#6cc29b` | 成功 toast、完成狀態 |
| 危險 Danger | `#b3372f` / hover `#992b24` | `#e08279` / hover `#b23e34` | 刪除、錯誤訊息、破壞性動作 |

收入、支出、淨值語意色不得任意替換。

## 白色模式（暖棉紙）

用於 App 內頁、表格、表單與長時間操作場景。

### 背景

| Token | 色碼 | 說明 |
|-------|------|------|
| App Background | `linear-gradient(180deg, #f8f5ef 0%, #f4f0e8 100%)` | 極微弱的暖色直向漸層 |
| Page Background | `#f7f4ee` | 一般頁面底色 |
| Surface | `#fffdf9` | 卡片、表格、表單主要底色 |
| Surface Glass | `rgba(255, 253, 249, 0.82)` | 玻璃質感卡片（保留給側欄、TopNav、Modal） |
| Glass Border | `rgba(38, 34, 28, 0.08)` | 近乎隱形的暖色 hairline |

### 文字

| Token | 色碼 | 說明 |
|-------|------|------|
| Text | `#26221c` | 主要文字、標題（暖黑） |
| Text Secondary | `#6b6157` | 次要文字、描述 |
| Text Muted | `#756a5d` | 補充資訊、placeholder、弱化標籤 |

### 邊框與陰影

| Token | 值 | 說明 |
|-------|----|------|
| Border | `#e6dfd3` | 表格分隔、input 邊框（暖灰） |
| Border Strong | `#d3c9b8` | input 邊框、表頭分隔 |
| Shadow | `0 3px 12px rgba(60, 45, 30, 0.06), 0 1px 3px rgba(60, 45, 30, 0.05)` | 一般卡片（暖中性，不用藍色陰影） |
| Shadow MD | `0 8px 26px rgba(60, 45, 30, 0.10), 0 2px 6px rgba(60, 45, 30, 0.06)` | 浮層、hover lift |
| Shadow LG | `0 14px 44px rgba(60, 45, 30, 0.14), 0 4px 12px rgba(60, 45, 30, 0.08)` | Modal、登入卡片 |

### 白色模式使用規則

- 主要操作使用 `--primary`（`#b0521c`），hover 使用 `--primary-dark`。
- 卡片優先 `--surface`；玻璃質感只給側欄、TopNav、Modal 等浮動表面。
- 長表格與表單白底，可讀性高於裝飾性。
- 收入、支出、淨值必用語意色。

## 黑色模式（暖夜）

不是反色：底色是暖黑，主色是亮化的赭色。用於使用者偏好深色介面。

### 背景

| Token | 色碼 | 說明 |
|-------|------|------|
| App Background | `#141210` | 純色暖黑，無漸層 |
| Surface | `#1d1a16` | 深色卡片、表單底色 |
| Surface Subtle | `#242019` | 次表面、chip |
| Surface Glass | `rgba(29, 26, 22, 0.82)` | 深色玻璃卡片 |
| Glass Border | `rgba(236, 231, 222, 0.10)` | 深色玻璃邊框 |

### 文字

| Token | 色碼 |
|-------|------|
| Text | `#ece7de`（暖白） |
| Text Secondary | `#b3a99c` |
| Text Muted | `#a89e91` |

### 暖夜使用規則

- 大面積背景接近黑的暖色，不使用純黑 `#000000`。
- 標題 `#ece7de`，內文 `#b3a99c`。
- 主色為亮赭 `#e2a377`（文字、連結、圖示）；實心按鈕維持深赭 `#a8481a`（`--primary-solid`）配白字。
- 卡片邊框一律 `white/10` 級透明度，不用高對比白線。

## 字體

| 用途 | 字體 | 說明 |
|------|------|------|
| 標題 Display | Fraunces（`--font-display`） | Dashboard 標題、section 卡標題、hero 金額 |
| 內文 / 數字 | Inter（`--font-sans`） | 全站內文；金額與表格數字加 `tabular-nums` |

以 `next/font/google` 於 `app/layout.tsx` 載入並 self-host，瀏覽器不對 Google 發請求。中文 fallback：Noto Sans TC / Noto Sans SC。避免對中文套負字距。

## 圓角階梯

| Token | 值 | 用途 |
|-------|-----|------|
| `--radius-sm` | 10px | input、小按鈕 |
| `--radius-md` | 12px | 小卡、chip |
| `--radius` | 16px | 卡片預設（`.card`、`.section-card`、`.table-wrap`） |
| `--radius-lg` | 20px | Modal、hero、登入卡 |

## 元件配色規則

### 按鈕

| 類型 | 白色模式 | 黑色模式 |
|------|----------|----------|
| Primary | `--primary-solid` 背景、白字 | 同左（深赭在暗底白字 5.16:1） |
| Primary Hover | `--primary-dark` | 同左 |
| Secondary | 表面底、`--border` 邊框、深色文字 | 透明底、`white/15` 邊框、淺色文字 |
| Danger | `--danger` + 白字 | danger tint，避免大面積實心紅 |

hover 不位移（不做 `translateY` 浮起），以背景深化 + 陰影擴張表達；`:active scale(0.98)`。

### 卡片

- App 卡片：`var(--surface-glass)`、`var(--glass-border)`、`var(--shadow-glass)`、`--radius`（16px）。
- 可點擊卡片才做 hover lift（`translateY(-2px)` + `--shadow-md`）；靜態卡不動。

### 表格

- 表頭：`0.75rem / 600`、`--text-muted`、無 UPPERCASE。
- 金額欄右對齊 + `tabular-nums`。
- 列分隔線：`color-mix(in srgb, var(--text) 8%, transparent)`。
- hover：`rgba(194, 87, 29, 0.035)`（極淡赭）。

### 提示框

| 類型 | 背景 | 文字 |
|------|------|------|
| Info | primary/sky tint ~12% | 對應淺色 |
| Success | `--success-bg` (12~16% tint) | `--income` |
| Warning | `--warning-bg` | `--today-text` |
| Error | `--danger-bg` | `--danger` |

## 圖表色盤（ReportsClient）

`['#b0521c', '#1e6b52', '#8a5a1f', '#b3372f', '#d98a4a', '#5f8d7a', '#c98a3d', '#9c4a3a', '#7d9464', '#a8683a']` — 暖赭為首，接語意色，之後為暖色階輪替。

## 對比驗證

```bash
npm run check:contrast
```

以 `app/globals.css` 的 token 值自動驗證雙主題 30 組真實組合（文字、語意色、badge、白字按鈕等）全數 ≥ 4.5:1。改 token 值後必須重跑。

## 不建議使用

- 不要使用純黑 `#000000` 或純白 `#ffffff` 大面積背景（暗模式用 `#141210`，卡片近白）。
- 不要把語意色（收入/支出/淨值）改成其他顏色。
- 不要在按鈕或卡片 hover 加位移動畫（`translate-y`）——背景深化即可。
- 不要在同一張卡片混用多個 accent，最多一個主 accent。
- 不要對內文（中文）套負字距；負字距只限拉丁 display 標題。
- 不要新增 Font Awesome 圖示——全站使用 lucide-react。

## 實作來源

- `app/globals.css` — `@theme`、`:root`、`.dark-mode` token 唯一來源。
- `app/layout.tsx` — next/font（Fraunces + Inter）載入。
- `lib/themeScript.ts` — 主題進場 script 單一來源（只切 `.dark-mode` class）。
- `hooks/useTheme.ts` — 執行期主題切換（與 themeScript 行為一致）。
- `app/dashboard/page.tsx` — 暖赭 hero 與訊號列。
- `tools/check-contrast.mjs` — 對比自動驗證。